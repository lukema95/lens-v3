You call configure(...) on the rule...

The rule returns (bytes4[] memory)...

So the rule basically returns the selectors of the primitive functions where it should be used

OR MAYBE MIGHT BE BETTER...

The rule basically returns the selectors of the rule functions it implements

------
MAYBE EVEN BETTER:
------

When you add the rule, you specify for which selectos you want this rule...
Then you save it to some mapping selector => mapping andRules/orRules or smth
