// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract RuleCombinator {
    /*
        What we want from this RuleCombinator?

        1) We want it to accept multiple rules
        2) We want it to combine rules using AND/OR and other complex shit
        3) We want it to support permissions natively and set each rule for permission
        4) We want it to support all kinds of functions natively (via signatures or names)

    */
}
