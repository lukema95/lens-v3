Option #1:

All rules are called through DELEGATECALL, either with or without the combinator on top.

Option #2:

Rules can be called by DELEGATECALL or just normal CALL.


===

Rules are deployed each time they are used VS. Rules are deployed once and initilized each time they are used
